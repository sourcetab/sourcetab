import * as presets from '~/presets';

export const defaultSettings: Settings = {
  theme: presets.themes.Default,
  layout: presets.layouts.Default,
  page: {
    pageTitle: 'Sourcetab',
    showHelp: true,
    linkTarget: '_self',
  },
};
